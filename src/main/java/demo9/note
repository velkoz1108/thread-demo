 void interrupt()              : 让线程变为中断状态的方法
 static boolean interrupted()  : 检查并清除中断状态的方法  静态方法  只能操作当前线程，不能清除其他线程的中断状态
 boolean isInterrupted()       : 检查指定线程的中断状态。若指定线程处于中断状态，则返回true，若处于非中断状态，则返回false。不会改变线程中断状态